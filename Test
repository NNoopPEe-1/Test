local Players = game:GetService("Players")

function kickPlayer(player, reason)
    player:Kick(reason)
end

-- Example usage:
local playerToKick = Players.LocalPlayer -- Replace with the target player

kickPlayer(playerToKick, "You have been kicked for breaking the rules.")
