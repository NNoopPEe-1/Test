local Players = game:GetService("Players")

function kickPlayer(player, reason)
    player:Kick(reason)
end

-- Example usage:
local playerToKick = Players.LocalPlayer -- Replace with the target player

kickPlayer(playerToKick, "ควายเอ้ย ควยไอ่เหี้ยเก้า ชนาภัทร ไอ่ควยเล็ก มึงนี่โง่จริงๆโดนกุหลอก")
